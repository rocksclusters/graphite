NAME		= graphite-config
RELEASE		= 0
VERSION		= 0.9.15
GRAPHITE_VERSION = $(VERSION)
CARBON_VERSION = $(GRAPHITE_VERSION)
TAR_POSTFIX	= tar.gz
GRAPHITE = graphite-web
CARBON =   carbon
#RPM.PREFIX	= /etc/init.d
RPM.FILESLIST = fileslist

